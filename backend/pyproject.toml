[project]
name = "faso-love-api"
version = "0.1.0"
description = "API FASO LOVE — plateforme de rencontre (18+) pour le Burkina Faso"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
