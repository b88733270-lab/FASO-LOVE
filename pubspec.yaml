name: faso_love
description: "FASO LOVE — application de rencontre (18+) pour le Burkina Faso."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.8.0
  provider: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/placeholders/

# NOTE — Origine du code : ce produit intègre du code du projet open-source
# « SparkMatch » (licence MIT, © 2025 Harendra Prajapati). Voir LICENSE et
# NOTICE.md. Les notices de licence sont conservées conformément aux termes.
